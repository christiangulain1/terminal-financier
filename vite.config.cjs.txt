import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react' 
import deploy from 'vite-plugin-gh-pages'

export default defineConfig({
  plugins: [react(), deploy()],
  base: '/terminal-financier/' 
})
